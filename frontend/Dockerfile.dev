FROM node:18-alpine
WORKDIR /app

# Ensure reproducible installs in container
ENV CI=true

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 3000

# Vite dev server, listening on all interfaces for Docker networking
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]


